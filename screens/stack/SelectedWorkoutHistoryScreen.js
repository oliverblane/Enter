import React from "react";
import { View, Text, StyleSheet } from "react-native";

const SelectedWorkoutHistoryScreen = (props) => {
  return <Text>Workout History</Text>;
};

const styles = StyleSheet.create({});

export default SelectedWorkoutHistoryScreen;
